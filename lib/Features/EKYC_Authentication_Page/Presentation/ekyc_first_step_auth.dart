import 'package:flutter/material.dart';

class EkycFirstStepAuth extends StatelessWidget {
  const EkycFirstStepAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
